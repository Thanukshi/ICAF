const sum = require('../Navbar/NavbarItem/Home/Home');

// test('password length is more than 8', () => {
//     expect(ouncesPerCan()).toBeGreaterThan(8);
//   });


test('bloop returns null', () => {
    expect(bloop()).toBeNull();
});
